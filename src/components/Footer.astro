---
import { useTranslations, localizedPath, type Locale } from '../i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const year = new Date().getFullYear();

function withBase(p: string) {
  return `${base}${localizedPath(locale, p)}`;
}

const navItems = [
  { key: 'about', href: '/about/' },
  { key: 'education', href: '/education/' },
  { key: 'campus', href: '/campus/' },
  { key: 'access', href: '/access/' },
  { key: 'contact', href: '/contact/' },
] as const;
---

<footer class="mt-24 border-t border-outline-variant bg-surface-container-lowest">
  <div class="container-wide grid gap-10 py-14 md:grid-cols-3">
    <div>
      <p class="font-serif text-lg leading-snug text-on-surface">{t.siteName}</p>
      <p class="mt-1 font-sans text-[11px] uppercase tracking-[0.05em] text-on-surface-variant">
        Kamiyama Marugoto College of Technology
      </p>
      <p class="mt-4 max-w-sm text-sm leading-relaxed text-on-surface-variant">
        {t.description}
      </p>
    </div>

    <nav>
      <p class="eyebrow">Sitemap</p>
      <ul class="mt-3 space-y-2 text-sm">
          <a class="link-underline text-on-surface-variant hover:text-on-surface" href={withBase('/')}>{t.nav.home}</a></li>
        {navItems.map((item) => (
          <li>
            <a class="link-underline text-on-surface-variant hover:text-on-surface" href={withBase(item.href)}>
              {t.nav[item.key]}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div>
      <p class="eyebrow">Links</p>
      <ul class="mt-3 space-y-2 text-sm">
        <li>
          <a
            class="link-underline text-on-surface-variant hover:text-on-surface"
            href="https://kamiyama.ac.jp/"
            target="_blank"
            rel="noopener noreferrer"
          >
            {t.footer.officialLabel} (kamiyama.ac.jp) →
          </a>
        </li>
        <li class="text-xs text-on-surface-variant">{t.footer.imageCredit}</li>
      </ul>
    </div>
  </div>

  <div class="border-t border-outline-variant">
    <div
      class="container-wide flex flex-col gap-2 py-5 text-xs text-on-surface-variant md:flex-row md:items-center md:justify-between"
    >
      <p>© {year} ghcp-school. {t.footer.rights}</p>
      <p class="font-sans tracking-[0.05em] uppercase">Kamiyama · Tokushima · Japan</p>
    </div>
  </div>
</footer>
